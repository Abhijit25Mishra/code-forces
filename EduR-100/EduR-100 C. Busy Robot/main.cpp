#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin>>t;
    while(t--){
        long long int n;
        cin>>n;
        long long int t[n],x[n];
        for(int i=0;i<n;i++){
            cin>>t[i]>>x[i];
        }
        sort(t,t+n);
        sort(x,x+n);
        long long int time,pos_x,i;
        i=0
        time=0;
        pos_x=0;

        if(t[i]==t){
            t++;

        }
        else if(z=100){

        }
        else {t++;}

        for(int i=0;i<n;i++){

        }

    }
    return 0;
}
